name: Publish to PyPI

on:
  push:
    branches: [main]
    paths:
      - 'cli/**'
      - 'pyproject.toml'
      - 'CLI.md'

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Bump patch version
        id: bump
        run: |
          # Read current version
          current=$(python -c "import re; m=re.search(r\"__version__\s*=\s*['\"](.+?)['\"]\", open('cli/__init__.py').read()); print(m.group(1))")
          echo "Current version: $current"

          # Bump patch
          IFS='.' read -r major minor patch <<< "$current"
          new_version="$major.$minor.$((patch + 1))"
          echo "New version: $new_version"

          # Write back
          sed -i "s/__version__ = '$current'/__version__ = '$new_version'/" cli/__init__.py
          echo "version=$new_version" >> "$GITHUB_OUTPUT"

      - name: Generate CLI docs
        run: |
          pip install requests
          python -c "
          import subprocess, textwrap

          commands = ['setup', 'login', 'up', 'get', 'rm', 'mv', 'renew', 'ls', 'status']
          lines = ['# drp CLI reference\n']

          # Main help
          result = subprocess.run(['python', '-m', 'cli.drp', '--help'], capture_output=True, text=True)
          lines.append('## Overview\n')
          lines.append('\`\`\`')
          lines.append(result.stdout.strip())
          lines.append('\`\`\`\n')

          # Each subcommand
          for cmd in commands:
              result = subprocess.run(['python', '-m', 'cli.drp', cmd, '--help'], capture_output=True, text=True)
              lines.append(f'## drp {cmd}\n')
              lines.append('\`\`\`')
              lines.append(result.stdout.strip())
              lines.append('\`\`\`\n')

          with open('docs/cli-reference.md', 'w') as f:
              f.write('\n'.join(lines))
          "

      - name: Commit version bump & docs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add cli/__init__.py docs/
          git diff --cached --quiet && exit 0
          git commit -m "v${{ steps.bump.outputs.version }}"
          git tag "v${{ steps.bump.outputs.version }}"
          git push --follow-tags

      - name: Build package
        run: |
          pip install build
          python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_API_TOKEN }}
